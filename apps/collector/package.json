{
  "name": "@node-flow/collector",
  "version": "0.1.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": "./dist/index.js",
  "bin": {
    "node-flow-collector": "./dist/bin.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@node-flow/protocol": "0.1.0",
    "@node-flow/topology-engine": "0.1.0",
    "@types/express": "^5.0.1",
    "@types/ws": "^8.18.1",
    "express": "^5.1.0",
    "ws": "^8.18.1"
  }
}
