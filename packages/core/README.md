# @node-flow/core

Runtime APIs used by [`@node-flow/node`](https://www.npmjs.com/package/@node-flow/node) for optional
custom spans and architectural boundaries. Most users should install and import the main package
instead of depending on this package directly.
