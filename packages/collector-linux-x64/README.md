# @mshamed1/node-flow-collector-linux-x64

Platform runtime package used automatically by `@mshamed1/node-flow` on Linux x64.
Install the main NodeFlow package instead of depending on this package directly.
