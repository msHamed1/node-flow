{
  "name": "nodescope-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @nodescope/protocol build && yarn workspace @nodescope/topology-engine build && yarn workspace @nodescope/core build && yarn workspace @nodescope/instrumentation-node build && yarn workspace @nodescope/instrumentation-nestjs build && yarn workspace @nodescope/dashboard build && yarn workspace @nodescope/collector build && yarn workspace nodescope build && yarn workspace @nodescope/demo-nestjs build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "yarn workspaces run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "demo": "yarn build && node packages/cli/dist/index.js dev -- node apps/demo-nestjs/dist/main.js",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
