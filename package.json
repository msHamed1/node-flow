{
  "name": "node-flow-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @node-flow/protocol build && yarn workspace @node-flow/topology-engine build && yarn workspace @node-flow/core build && yarn workspace @node-flow/instrumentation-node build && yarn workspace @node-flow/instrumentation-nestjs build && yarn workspace @node-flow/dashboard build && yarn workspace @node-flow/collector build && yarn workspace @node-flow/node build && yarn workspace @node-flow/demo-nestjs build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "yarn workspaces run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "demo": "yarn build && node packages/cli/dist/cli.js dev -- node apps/demo-nestjs/dist/main.js",
    "clean": "yarn workspaces run clean"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/msHamed1/node-flow.git"
  },
  "homepage": "https://github.com/msHamed1/node-flow#readme",
  "devDependencies": {
    "@types/node": "^22.15.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
