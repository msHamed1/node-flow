{
  "name": "node-flow-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @mshamed1/node-flow-protocol build && yarn workspace @mshamed1/node-flow-integration-contracts build && yarn workspace @mshamed1/node-flow-topology-engine build && yarn workspace @mshamed1/node-flow-core build && yarn workspace @mshamed1/node-flow-instrumentation-node build && yarn workspace @mshamed1/node-flow-instrumentation-nestjs build && yarn workspace @mshamed1/node-flow-dashboard build && yarn workspace @mshamed1/node-flow-collector build && yarn workspace @mshamed1/node-flow build && yarn workspace @mshamed1/node-flow-demo-nestjs build && yarn workspace @mshamed1/node-flow-mock-risk-service build && yarn workspace @mshamed1/node-flow-integration-api build && yarn workspace @mshamed1/node-flow-integration-worker build",
    "test": "vitest run",
    "test:golden": "vitest run packages/topology-engine/test/golden-corpus.test.ts",
    "test:watch": "vitest",
    "lint": "yarn workspaces run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "demo": "yarn build && node packages/cli/dist/cli.js dev -- node apps/demo-nestjs/dist/main.js",
    "demo:traffic": "node scripts/generate-demo-traffic.mjs",
    "clean": "yarn workspaces run clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "yarn release:check && changeset publish",
    "release:check": "node scripts/check-release-readiness.mjs",
    "scope:check": "node scripts/check-scope-migration.mjs",
    "package:check": "node scripts/check-packages.mjs",
    "package:smoke": "node scripts/smoke-packed-package.mjs",
    "integration:up": "docker compose up -d --build --wait --wait-timeout 240",
    "integration:down": "docker compose down -v --remove-orphans",
    "integration:test": "node scripts/integration-test.mjs",
    "integration:durability": "node scripts/durable-recovery-test.mjs",
    "integration:logs": "docker compose logs -f nodeflow-collector nodeflow integration-api integration-worker mock-risk-service",
    "integration": "yarn integration:up && yarn integration:test && yarn integration:durability",
    "proto:generate": "scripts/generate-protocol.sh",
    "proto:check": "yarn proto:generate && git diff --exit-code -- services/collector/gen",
    "go:build": "cd services/collector && go build ./...",
    "go:test": "cd services/collector && go test ./...",
    "go:race": "cd services/collector && go test -race ./...",
    "go:vet": "cd services/collector && go vet ./...",
    "go:benchmark": "cd services/collector && go test -run '^$' -bench BenchmarkPipeline -benchmem ./internal/pipeline"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/msHamed1/node-flow.git"
  },
  "homepage": "https://github.com/msHamed1/node-flow#readme",
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@types/node": "^22.15.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
