# @mshamed1/node-flow-collector-darwin-x64

Platform runtime package used automatically by `@mshamed1/node-flow` on Intel macOS.
Install the main NodeFlow package instead of depending on this package directly.
