# @mshamed1/node-flow-collector-darwin-arm64

Platform runtime package used automatically by `@mshamed1/node-flow` on macOS Apple Silicon.
Install the main NodeFlow package instead of depending on this package directly.
