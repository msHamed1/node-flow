# @mshamed1/node-flow-collector-linux-arm64

Platform runtime package used automatically by `@mshamed1/node-flow` on Linux ARM64.
Install the main NodeFlow package instead of depending on this package directly.
