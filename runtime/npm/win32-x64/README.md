# @mshamed1/node-flow-collector-win32-x64

Platform runtime package used automatically by `@mshamed1/node-flow` on Windows x64.
Install the main NodeFlow package instead of depending on this package directly.
